module Gcd
import Nat
import Arith as A
use Nat.P
use A.IsZero
export Mod, GCD

@Mod(a,b) :=
  [ b = 0 -> 0;
    a < b -> a;
    else  -> @Mod(a - b, b)
  ]

@GCD(a,b) :=
  [ IsZero(b) -> a; 
    else      -> @GCD(b, @Mod(a,b))
  ]
