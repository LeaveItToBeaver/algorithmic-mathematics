module Arith
import Nat
use Nat.S, Nat.P, Nat.Add
export Mul, Pow, IsZero

@IsZero(x) := [ x = 0 -> true ; else -> false ]

@Mul(a,b) :=
  [ b = 0 -> 0;
    else  -> Add(a, @Mul(a, P(b)))
  ]

@Pow(a,n) :=
  [ n = 0 -> 1;
    else  -> @Mul(a, @Pow(a, P(n)))
  ]
