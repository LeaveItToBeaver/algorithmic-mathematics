module Nat
export S, P, Add, Sub

@S(x) := x + 1
@P(x) := [ x = 0 -> 0 ; else -> x - 1 ]

@Add(a,b) :=
  [ b = 0 -> a;
    else  -> S(@Add(a, P(b)))
  ]

@Sub(a,b) :=
  [ b = 0 -> a;
    else  -> @Sub(P(a), P(b))
  ]
